spring:
  cloud:
    config:
#      name: auth, myjwt, rabbitmq, service-registry-client, sleuth_zipkin
      name: auth, myjwt, service-registry-client, sleuth_zipkin
      uri: http://config-server:10010


logging:
  level:
#    org.springframework.web: DEBUG
    org.springframework.cloud.sleuth: DEBUG