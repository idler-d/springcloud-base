spring:
  cloud:
    config:
      name: user, mysql, redis, rabbitmq, service-registry-client
      uri: http://config-server:10010
#  cache:
#    type: redis
logging:
  level:
    com.cet.diagsyscloud.system.dao: DEBUG
    com.idler.demo.user.mapper: DEBUG
